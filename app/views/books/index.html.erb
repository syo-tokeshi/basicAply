<h1 class="text-danger">みんなが好きな本一覧！</h1>
<br>
  <table class="table table-hover text-success">
   <tbody>
    <thead>
      <tr>
        <th>タイトル</th>
        <th>カテゴリー</th>
        <th>コメント</th>
        <th>値段</th>

      </tr>
    </thead>
  <% @books.each do |book| %>
    <tr>
      <td><%= link_to book.title,book_path(book) %></td>
      <td><%= link_to book.category,book_path(book) %></td>
      <td><%= link_to book.comment,book_path(book) %></td>
      <td><%= link_to book.price,book_path(book) %>円</td>
      
    </tr>
  <% end %>
   </tbody>
</table>

<br>
<br>
<br>
<h2>本についてあれこれ！</h2>
<br>
<h2>おすすめされている本の平均価格は？</h2>
<h2 class="text-danger">→<%= @books_average.floor %>円</h2>
<br>
<h2 >一番安い本は？</h2>
<br>
  <table class="table table-hover text-success">
   <tbody>
    <thead>
      <tr>
        <th>タイトル</th>
        <th>値段</th>
      </tr>
    </thead>
    <tr>
      <td><%= @minimum_book.title %></td>
      <td class="text-danger"><%= @minimum_book.price %>円</td>
    </tr>
   </tbody>
</table>

<h2 >じゃあ一番高い本は？</h2>
<table class="table table-hover text-success">
 <tbody>
  <thead>
    <tr>
      <th>タイトル</th>
      <th>値段</th>
    </tr>
  </thead>
  <tr>
    <td><%= @miximum_book.title %></td>
    <td class="text-danger"><%= @miximum_book.price %>円</td>
  </tr>
 </tbody>
</table>
<%= paginate @books %>
