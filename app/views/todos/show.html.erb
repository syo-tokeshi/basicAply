<h1>todoリスト</h1>
<p>本日の予定：<%= @todo.today %></p>

<% if @todo.id == @current_user.id %>
  <p><%= link_to "プロフィールを編集する", edit_todo_path(@todo) %></p>
  <p><%= link_to "退会する", todo_path(@todo),method: :delete,data: {confirm: "退会して大丈夫ですか?"}  %></p>
<% end %>
